import { Check, RotateCcw, X } from "lucide-react";
import React from "react";
import { useChatTheme } from "../theme/ChatThemeProvider";
import type { ChatThemeMode } from "../theme/chatThemes";
const MODES: {id:ChatThemeMode;label:string;colors:string[]}[]=[{id:"lab",label:"Follow Lab Theme",colors:["var(--color-primary)","var(--color-secondary)"]},{id:"light",label:"Clean Light",colors:["#fff","#0284c7"]},{id:"dark",label:"Dark",colors:["#111827","#38bdf8"]},{id:"monochrome",label:"Monochrome",colors:["#fff","#262626"]},{id:"midnight",label:"Midnight",colors:["#050816","#818cf8"]},{id:"soft-blue",label:"Soft Blue",colors:["#edf5fb","#2583c5"]}];
const ChatSettingsDrawer:React.FC<{open:boolean;onClose:()=>void}>=({open,onClose})=>{const {appearance,setAppearance,reset}=useChatTheme();if(!open)return null;return <div className="fixed inset-0 z-[2147483300] bg-slate-950/55 backdrop-blur-sm" onMouseDown={onClose}><aside className="ml-auto h-full w-full max-w-md overflow-y-auto bg-white p-6 shadow-2xl" onMouseDown={(event)=>event.stopPropagation()}><div className="flex items-center justify-between"><div><p className="text-[10px] font-black uppercase tracking-widest text-sky-600">Personal preference</p><h2 className="mt-1 text-2xl font-black text-slate-900">Chat appearance</h2></div><button onClick={onClose} className="grid h-10 w-10 place-items-center rounded-full bg-slate-100"><X size={18}/></button></div><p className="mt-2 text-sm leading-6 text-slate-500">These settings apply only on this device. Follow Lab Theme updates automatically with the admin theme.</p><div className="mt-6 grid grid-cols-2 gap-3">{MODES.map((mode)=><button key={mode.id} onClick={()=>setAppearance((current)=>({...current,mode:mode.id}))} className={`rounded-2xl border p-3 text-left ${appearance.mode===mode.id?"border-sky-500 ring-4 ring-sky-100":"border-slate-200"}`}><div className="flex h-10 overflow-hidden rounded-xl">{mode.colors.map((color,index)=><span key={index} className="flex-1" style={{background:color}}/>)}</div><div className="mt-2 flex items-center justify-between"><span className="text-xs font-black text-slate-700">{mode.label}</span>{appearance.mode===mode.id&&<Check size={15} className="text-sky-600"/>}</div></button>)}</div><div className="mt-6 space-y-3 border-t border-slate-100 pt-5"><Setting label="Compact message spacing" checked={appearance.density==="compact"} onChange={(checked)=>setAppearance((current)=>({...current,density:checked?"compact":"comfortable"}))}/><Setting label="Conversation background pattern" checked={appearance.pattern} onChange={(pattern)=>setAppearance((current)=>({...current,pattern}))}/><Setting label="Reduce motion" checked={appearance.reduceMotion} onChange={(reduceMotion)=>setAppearance((current)=>({...current,reduceMotion}))}/></div><button onClick={reset} className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-slate-100 px-4 py-3 text-sm font-black text-slate-600"><RotateCcw size={16}/>Reset to Lab Theme</button></aside></div>;};
const Setting:React.FC<{label:string;checked:boolean;onChange:(value:boolean)=>void}>=({label,checked,onChange})=><label className="flex items-center justify-between rounded-xl border border-slate-200 p-3 text-sm font-bold text-slate-700"><span>{label}</span><input type="checkbox" checked={checked} onChange={(event)=>onChange(event.target.checked)} className="h-5 w-5 accent-sky-600"/></label>;
export default ChatSettingsDrawer;
