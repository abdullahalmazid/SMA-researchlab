import React from "react";
import AdminModal from "./AdminModal";
const ConfirmDialog: React.FC<{ open: boolean; title: string; message: string; confirmLabel?: string; busy?: boolean; danger?: boolean; onCancel: () => void; onConfirm: () => void }> = ({ open, title, message, confirmLabel = "Confirm", busy, danger, onCancel, onConfirm }) => <AdminModal open={open} title={title} description={message} onClose={onCancel} width="480px" footer={<div className="flex justify-end gap-3"><button onClick={onCancel} disabled={busy} className="rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-bold text-slate-700">Cancel</button><button onClick={onConfirm} disabled={busy} className={`rounded-xl px-5 py-2.5 text-sm font-black text-white ${danger ? "bg-rose-600" : "bg-indigo-600"}`}>{busy ? "Working…" : confirmLabel}</button></div>}><p className="text-sm leading-6 text-slate-600">This action will be applied immediately.</p></AdminModal>;
export default ConfirmDialog;
